# Neovim Cheat Sheet - Quick Reference

**Leader Key:** `<Space>` (press space to see all available commands)

---

## 🚀 Getting Started

### Opening Neovim
```bash
nvim                    # Open Neovim
nvim filename.txt       # Open specific file
nvim .                  # Open in current directory
```

### Exiting Neovim
- `:q` - Quit (fails if unsaved changes)
- `:q!` - Quit without saving
- `:w` - Save file
- `:wq` or `ZZ` - Save and quit
- `<Ctrl-s>` - Quick save

---

## 📝 Basic Editing

### Modes
- `i` - Insert mode (before cursor)
- `a` - Insert mode (after cursor)
- `A` - Insert at end of line
- `o` - Insert new line below
- `O` - Insert new line above
- `<Esc>` - Return to Normal mode
- `v` - Visual mode (select text)
- `V` - Visual Line mode

### Movement
- `h` `j` `k` `l` - Left, Down, Up, Right
- `w` - Next word
- `b` - Previous word
- `0` - Start of line
- `$` - End of line
- `gg` - Top of file
- `G` - Bottom of file
- `{` `}` - Previous/Next paragraph
- `<Ctrl-u>` - Page up
- `<Ctrl-d>` - Page down

### Editing
- `x` - Delete character
- `dd` - Delete line
- `yy` - Copy (yank) line
- `p` - Paste after cursor
- `P` - Paste before cursor
- `u` - Undo
- `<Ctrl-r>` - Redo
- `.` - Repeat last command

---

## 🗂️ File Explorer (nvim-tree)

### Opening File Explorer
- `<Space>e` - Toggle file explorer

### Inside File Explorer
- `<Enter>` or `o` - Open file/folder
- `a` - Create new file
- `d` - Delete file
- `r` - Rename file
- `x` - Cut file
- `c` - Copy file
- `p` - Paste file
- `R` - Refresh tree
- `H` - Toggle hidden files
- `?` - Show help

---

## 🔍 Finding Things (Telescope)

### File & Text Search
- `<Space>ff` - Find files (fuzzy search)
- `<Space>fg` - Live grep (search in files)
- `<Space>fb` - Find buffers (open files)
- `<Space>fh` - Help tags
- `<Space>fr` - Recent files
- `<Space>fc` - Find word under cursor
- `<Space>fk` - Find keymaps

### Inside Telescope
- `<Ctrl-n>` / `<Ctrl-p>` - Next/Previous result
- `<Enter>` - Open file
- `<Esc>` - Close Telescope
- `<Ctrl-x>` - Open in horizontal split
- `<Ctrl-v>` - Open in vertical split

---

## 🪟 Windows & Buffers

### Window Navigation
- `<Ctrl-h>` - Move to left window
- `<Ctrl-j>` - Move to bottom window
- `<Ctrl-k>` - Move to top window
- `<Ctrl-l>` - Move to right window

### Window Management
- `<Ctrl-Up>` - Increase window height
- `<Ctrl-Down>` - Decrease window height
- `<Ctrl-Left>` - Decrease window width
- `<Ctrl-Right>` - Increase window width
- `:split` or `<Ctrl-w>s` - Horizontal split
- `:vsplit` or `<Ctrl-w>v` - Vertical split

### Buffer Navigation
- `<Shift-h>` - Previous buffer
- `<Shift-l>` - Next buffer
- `<Space>bd` - Delete buffer

---

## 💡 LSP (Code Intelligence)

### Navigation
- `gd` - Go to definition
- `gD` - Go to declaration
- `gi` - Go to implementation
- `gr` - Go to references
- `K` - Show hover documentation
- `<Ctrl-k>` - Signature help (in Insert mode)

### Code Actions
- `<Space>ca` - Code actions
- `<Space>rn` - Rename symbol
- `<Space>f` - Format document

### Diagnostics (Errors/Warnings)
- `[d` - Previous diagnostic
- `]d` - Next diagnostic
- `<Space>d` - Show diagnostic details
- `<Space>q` - Open diagnostic list

---

## ✏️ Auto-completion (nvim-cmp)

### In Insert Mode
- `<Ctrl-Space>` - Trigger completion
- `<Tab>` - Next item / expand snippet
- `<Shift-Tab>` - Previous item
- `<Enter>` - Confirm selection
- `<Ctrl-e>` - Abort completion
- `<Ctrl-f>` - Scroll docs forward
- `<Ctrl-b>` - Scroll docs backward

---

## 🤖 GitHub Copilot (AI Assistance)

### Setup
Run `:Copilot setup` on first use and authenticate with your GitHub account.

### In Insert Mode
- `<Tab>` - Accept Copilot suggestion
- `<Alt-]>` - Next suggestion
- `<Alt-[>` - Previous suggestion
- `<Alt-\>` - Dismiss suggestion

**Note:** Copilot suggestions appear as ghost text while typing. Press Tab to accept or keep typing to ignore.

---

## 🎯 Git Integration (gitsigns)

### Git Hunks
- `]c` - Next git change (hunk)
- `[c` - Previous git change (hunk)
- `<Space>gs` - Stage hunk
- `<Space>gr` - Reset hunk
- `<Space>gp` - Preview hunk
- `<Space>gb` - Show git blame

---

## 📋 Text Objects & Selection

### Visual Selection
- `viw` - Select inner word
- `vaw` - Select around word
- `vi"` - Select inside quotes
- `va"` - Select around quotes
- `vi(` - Select inside parentheses
- `va(` - Select around parentheses

### Treesitter Text Objects
- `vaf` - Select around function
- `vif` - Select inside function
- `vac` - Select around class
- `vic` - Select inside class

---

## 🔧 Useful Commands

### File Operations
- `:e filename` - Edit/open file
- `:w` - Save file
- `:w filename` - Save as
- `:r filename` - Read file into current buffer

### Search & Replace
- `/pattern` - Search forward
- `?pattern` - Search backward
- `n` - Next search result
- `N` - Previous search result
- `:%s/old/new/g` - Replace all in file
- `:s/old/new/g` - Replace in current line

### System
- `:checkhealth` - Check Neovim health
- `:Mason` - Open LSP server manager
- `:Lazy` - Open plugin manager
- `:LspInfo` - Show LSP status

---

## 💻 Terminal

### Opening Terminal
- `:terminal` or `:term` - Open terminal in current window
- `:split | terminal` - Open terminal in horizontal split
- `:vsplit | terminal` - Open terminal in vertical split

### In Terminal Mode
- `<Ctrl-\><Ctrl-n>` - Exit terminal mode (back to normal mode)

---

## 🎨 Customization Commands

### Theme
- `:colorscheme tokyonight` - Current theme
- `:colorscheme` - Show current theme

### Plugins
- `:Lazy` - Plugin manager
  - `U` - Update plugins
  - `S` - Sync plugins
  - `C` - Clean unused plugins

### LSP Servers
- `:Mason` - Package manager
  - `i` - Install server
  - `u` - Update server
  - `X` - Uninstall server

---

## 🆘 Help System

- `:help <topic>` - Get help on any topic
- `:help lsp` - LSP help
- `:help telescope` - Telescope help
- `:Tutor` - Interactive Vim tutorial
- `<Space>` - Press space to see all leader key commands

---

## 📚 Learning Resources

1. **Built-in Tutorial:** Run `:Tutor` in Neovim
2. **Plugin Help:** Use `:help <plugin-name>`
3. **LSP Info:** Use `:LspInfo` to check language servers
4. **Health Check:** Use `:checkhealth` to diagnose issues
5. **Which-key:** Press `<Space>` and wait to see available commands

---

## 🔥 Pro Tips

1. **Press `<Space>` and wait** - which-key will show you all available commands
2. **Use `K` over symbols** - Shows documentation instantly
3. **Use `gd` to jump to definitions** - Navigate code quickly
4. **Use `<Space>ff`** - Fuzzy find files faster than file explorer
5. **Use `<Space>fg`** - Search text across all files
6. **Press `?` in any plugin** - Most plugins have contextual help

---

---

## 🪟 Window Navigation Guide

### Quick Navigation Between Windows

When you have multiple windows open (created with splits), use these keybindings to move between them:

```
┌─────────────────┐
│   <Ctrl-k>      │ Top window
│      (up)       │
├─────┬───────────┤
│Ctrl │  Current  │ Ctrl-l
│ -h  │  Window   │ (right)
│(←)  │           │
├─────┴───────────┤
│  <Ctrl-j>       │ Bottom window
│   (down)        │
└─────────────────┘
```

**Window Navigation Keys:**
- `<Ctrl-h>` - Move to the **left** window
- `<Ctrl-j>` - Move to the **bottom** window
- `<Ctrl-k>` - Move to the **top** window
- `<Ctrl-l>` - Move to the **right** window

### Example Workflow

1. Open a file: `nvim myfile.txt`
2. Create a vertical split: `<Ctrl-w>v` or `:vsplit`
3. Move right to the new window: `<Ctrl-l>`
4. Open another file in the right pane
5. Move back left: `<Ctrl-h>`
6. Use `<Ctrl-j>` and `<Ctrl-k>` to navigate between top/bottom splits

### Creating and Managing Splits

- `:split` or `<Ctrl-w>s` - Create horizontal split (top/bottom)
- `:vsplit` or `<Ctrl-w>v` - Create vertical split (left/right)
- `<Ctrl-w>c` - Close current window
- `<Ctrl-w>o` - Close all other windows (keep only current)

### Resizing Windows

Once you're in a window, resize it with:
- `<Ctrl-Up>` - Increase height
- `<Ctrl-Down>` - Decrease height
- `<Ctrl-Left>` - Decrease width
- `<Ctrl-Right>` - Increase width

### Pro Tip: Window Navigation with Vim Motions

Remember: The navigation keys follow Vim motion directions:
- `<Ctrl-h>` = move **h**orizontal left
- `<Ctrl-j>` = move down (picture the 'j' as a hook)
- `<Ctrl-k>` = move up
- `<Ctrl-l>` = move right

Combining `Ctrl` with these gives you window navigation! 

---

**Remember:** This is your custom Neovim setup from your dotfiles repository!
For more details, see: `~/.config/nvim/README.md`
